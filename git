#!/bin/bash

# Check if a message is provided as an argument
if [ -z "$1" ]; then
  # If no argument is provided, use current date and time as the commit message
  COMMIT_MESSAGE="Commit on"
else
  # If an argument is provided, use it as the commit message
  COMMIT_MESSAGE="$1"
fi

# Execute git commands
git add .
git commit -m "feat: Integrate Google Maps functionality

- Add Google Maps Flutter dependency
- Configure API keys and permissions for Android and iOS
- Implement map view with custom study spot markers
- Add user location tracking functionality
- Set up camera position controls and zoom features
- Create custom map marker info windows
- Handle location permissions and service availability
- Add fallback for location services not available
- Document map integration process

This commit enables the core map-based functionality for the StudySpot app, allowing users to visually browse study locations on an interactive map and see their own position relative to available study spots."
git push

# 409434