#!/bin/bash

# Check if a message is provided as an argument
if [ -z "$1" ]; then
  # If no argument is provided, use current date and time as the commit message
  COMMIT_MESSAGE="Commit on"
else
  # If an argument is provided, use it as the commit message
  COMMIT_MESSAGE="$1"
fi

# Execute git commands
git add .
git commit -m "feat: Implement core StudySpot application structure

- Add Firebase integration with authentication screens
- Create responsive home page with Google Maps
- Implement location-based study spot markers
- Add study spot data model and service
- Set up GetX for state management
- Implement interactive map markers with spot details
- Create onboarding experience
- Configure necessary dependencies for maps, auth, and storage
- Set up project structure following clean architecture principles

This commit establishes the foundation for the StudySpot app with authentication, GPS location detection, and map-based study spot discovery."
git push

# 409434