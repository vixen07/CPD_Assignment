#!/bin/bash

# Check if a message is provided as an argument
if [ -z "$1" ]; then
  # If no argument is provided, use current date and time as the commit message
  COMMIT_MESSAGE="Commit on"
else
  # If an argument is provided, use it as the commit message
  COMMIT_MESSAGE="$1"
fi

# Execute git commands
git add .
git commit -m "feat: Implement local notifications for liked study spots

- Add flutter_local_notifications package integration
- Create NotificationService class to handle notification logic
- Configure platform-specific notification settings for Android and iOS
- Trigger notification when user likes a study spot
- Add payload to navigate to spot details when notification is tapped
- Integrate with user settings for notification preferences
- Include proper documentation and testing across platforms

This commit satisfies the local notifications requirement in the project rubric by providing immediate feedback when users interact with study spots."
git push

